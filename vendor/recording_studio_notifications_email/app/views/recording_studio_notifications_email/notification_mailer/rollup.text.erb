<%= pluralize(@events.size, "notification") %>

<% @events.each do |event| %><%= event.title %>
<% if event.body.present? %><%= event.body %>
<% end %><% if event.url.present? %>View: <%= event.url %>
<% end %>
<% end %>
