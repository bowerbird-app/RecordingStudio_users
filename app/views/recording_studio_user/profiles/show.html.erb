<% content_for :title, "My Profile | Recording Studio User" %>

<% recording_studio_page_nav(
  title: "My Profile",
  page_nav_anchor_url: main_app.root_path,
  page_nav_anchor_label: "Close"
) %>

<%= render FlatPack::PageTitle::Component.new(
  title: "My Profile",
  subtitle: "Your name, email, and photo.",
  variant: :h1
) do |page_title| %>
  <% page_title.slot do %>
    <%= render FlatPack::Button::Component.new(
      text: "Edit",
      href: edit_profile_path,
      style: :primary
    ) %>
  <% end %>
<% end %>

<div class="space-y-4">
  <%= render "photo", return_to: profile_path %>

  <dl class="space-y-4">
    <div>
      <dt class="text-sm text-[var(--surface-muted-content-color)]">Name</dt>
      <dd class="mt-1 font-medium"><%= profile_display_name(@user) %></dd>
    </div>
    <div>
      <dt class="text-sm text-[var(--surface-muted-content-color)]">Email</dt>
      <dd class="mt-1 font-medium"><%= @user.email %></dd>
    </div>
    <div>
      <dt class="text-sm text-[var(--surface-muted-content-color)]">Time zone</dt>
      <dd class="mt-1 font-medium"><%= @profile.time_zone %></dd>
    </div>
  </dl>
</div>
